{
  "name": "miles-davis-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun run --bun vite build",
    "preview": "bun run --bun vite preview",
    "cms": "bun run --bun @webhouse/cms"
  },
  "dependencies": {
    "@webhouse/cms": "latest",
    "@webhouse/cms-cli": "^0.2.17",
    "preact": "^10.23.0",
    "preact-iso": "^2.9.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.5",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^5.4.0"
  }
}
